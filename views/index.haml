!!!
%html
  %head
    %link{"rel" => "stylesheet", "href" => "/css/style.css", "type" => "text/css"}
    %link{"rel" => "stylesheet", "href" => "http://fonts.googleapis.com/css?family=Lato:400,900", "type" => "text/css"}
    %title gitsize | #{@config[:username]}/#{@config[:repo]}
  %body
    %h1 GITSIZE
    %h2
      %span= @config[:username]
      \/
      %span= @config[:repo]
      \/
      %span= @config[:path]
    %h3
      Commit: #{@commit}
    %ul
      - @commit.gitcommitfiles.each do |commitfile|
        %li{:class => commitfile.size_class}
          = commitfile.gitfile
          %small (#{commitfile.friendly_size})